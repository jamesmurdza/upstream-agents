/**
 * OpenCode tool name mappings
 *
 * Maps OpenCode tool names to canonical tool names.
 */

export const OPENCODE_TOOL_MAPPINGS: Record<string, string> = {
  bash: "shell",
  // Most OpenCode tools use canonical names already
}
